{% extends 'base.html.twig' %}

{% block title %}Edit Columns{% endblock %}

{% block body %}
    <h1>Edit Columns</h1>

    {{ include('columns/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('columns_index') }}">back to list</a>

    {{ include('columns/_delete_form.html.twig') }}
{% endblock %}
